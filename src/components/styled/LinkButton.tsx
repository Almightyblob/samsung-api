import styled from "styled-components";

const LinkButton = styled.a.attrs({
  className: `bg-blue-600 rounded-full text-white font-semibold hover:bg-blue-700 duration-100 py-2 px-6`,
})``;

export default LinkButton;
